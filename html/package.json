{
  "name": "project",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "run:dev": "http-server . --ssl --cert ssl/self.cert --key ssl/self.key"
  },
  "dependencies": {
    "http-server": "^0.11.1"
  }
}
