general: {
	enabled = true
	host = "$RABBIT_HOST"
	port = $RABBIT_PORT
	username = "$RABBIT_USER"
	password = "$RABBIT_PASS"
	vhost = "$RABBIT_VHOST"
	to_janus = "$RABBIT_QUEUE_TO"
	from_janus = "$RABBIT_QUEUE_FROM"
}

admin: {
	admin_enabled = $RABBIT_ADMIN_ENABLE
	to_janus_admin = "$RABBIT_ADMIN_QUEUE_TO"
	from_janus_admin = "$RABBIT_ADMIN_QUEUE_FROM"
}
